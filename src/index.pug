extends layouts/main

block content
    +b-header(menuList, languagesList, socialList)
    +b-header(menuList, languagesList, socialList, '_dark')
    +b-header(menuList, languagesList, socialList, '_white')